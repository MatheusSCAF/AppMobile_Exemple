import { ComponentCardPipe } from './component-card.pipe';

describe('ComponentCardPipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentCardPipe();
    expect(pipe).toBeTruthy();
  });
});
